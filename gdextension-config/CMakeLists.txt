# Set the template and output file
set(CONFIG_TEMPLATE "${CMAKE_CURRENT_SOURCE_DIR}/template.gdextension.in")
set(CONFIG_OUTPUT "${CMAKE_BINARY_DIR}/${PROJECT_NAME}/${PROJECT_NAME}.gdextension")

set(GDEXTENSION_LIBRARY_FILE_NAME "${PROJECT_NAME}.${GDEXTENSION_LIBRARY_TYPE_NAME}${CMAKE_SHARED_LIBRARY_SUFFIX}")

configure_file(${CONFIG_TEMPLATE} ${CONFIG_OUTPUT} @ONLY)